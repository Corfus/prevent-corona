import { PolicyToIconPipe } from './policy-to-icon.pipe';

describe('PolicyToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PolicyToIconPipe();
    expect(pipe).toBeTruthy();
  });
});

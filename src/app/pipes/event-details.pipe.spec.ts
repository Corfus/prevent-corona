import { EventDetailsPipe } from './event-details.pipe';

describe('EventDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ActionToImagePipe } from './action-to-image.pipe';

describe('ActionToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ActionToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
